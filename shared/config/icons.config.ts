/**
 * Configuration for the application's icon system.
 */
export const ICON_CONFIG = {
  /**
   * The default prefix/set for icons.
   * Can be configured to 'material-symbols', 'tabler', 'lucide', 'heroicons', etc.
   * Make sure that the icons requested exist in the chosen set, or use
   * override notation 'prefix:icon-name' (e.g. 'lucide:home') when using different icon sets.
   */
  defaultPrefix: 'material-symbols',
};
